# Core runtime dependencies (mirrors pyproject.toml). Optional extras:
#   pip install -e ".[hf]"          local Hugging Face models (torch, transformers, accelerate)
#   pip install -e ".[data]"        public dataset loaders (datasets)
#   pip install -e ".[dashboard]"   interactive demo (gradio)
#   pip install -e ".[api]"         FastAPI service (fastapi, uvicorn)
#   pip install -e ".[embeddings]"  sentence-transformer embeddings for learned routers
#   pip install -e ".[xgboost]"     XGBoost classifier option
#   pip install -e ".[dev]"         tests, lint, type checking
numpy>=1.24
pydantic>=2.5
pyyaml>=6.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
